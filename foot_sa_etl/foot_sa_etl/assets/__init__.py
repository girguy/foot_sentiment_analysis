# This file is required to define the package.
# It ensures that assets in the assets folder are loaded correctly.
# To access the bronze, silver, and other sub-packages within the
# assets package, both the assets package and each of its sub-packages
# must have an __init__.py file, even if the file is empty,
# to designate them as proper Python packages.
